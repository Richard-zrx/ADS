# Core data-preprocessing pipeline (embedding -> cluster -> difficulty -> sort).
# Versions are pinned to our reference environment (Python 3.10.20). The same
# environment is used for the RL training step, so these are compatible with the
# verl training stack below.
#
# The RL training step has its own (heavier) dependencies; install those with
#   pip install -e verl
torch==2.10.0          # built against CUDA 12.9 (torch==2.10.0+cu129 in our env)
transformers==4.57.6
datasets==4.8.4
accelerate==1.13.0
numpy==2.1.3
pandas==2.3.3
pyarrow==23.0.1
scikit-learn==1.7.2
tqdm==4.67.3
PyYAML==6.0.3

# RL training stack — installed via `pip install -e verl`; versions listed here
# for reference / reproducibility (the exact versions in our environment):
#   vllm==0.17.1
#   ray==2.54.0
#   tensordict==0.10.0
#   flash-attn==2.8.3      (pip install flash-attn==2.8.3 --no-build-isolation)

# Optional: cluster visualization (src/visualize_clusters_*.py)
# matplotlib==3.10.8
# wordcloud

# Optional: alternative clustering methods (knn_leiden / hdbscan / umap configs)
# igraph
# leidenalg
# umap-learn
# hdbscan
